/** @type {import('next').NextConfig} */
const nextConfig = {
    // Define your configurations here
    images: {
        domains: ['img.freepik.com'], // Example domain for image optimization
    },
    // Other Next.js configurations can be added here
};

module.exports = nextConfig;
